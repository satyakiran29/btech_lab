ASSUME CS:CODE
DATA SEGMENT
org 2000h
        LIST DB 12H,34H,56H,78H,99H
DATA ENDS
EXTRA SEGMENT
    ORG 3000h
        RES DB 05H DUP(0)
EXTRA ENDS
CODE SEGMENT
START:MOV AX, DATA
       MOV DS, AX
       MOV AX, EXTRA
       MOV CS,AX
       MOV CX,05H
       MOV SI, OFFSET LIST
       MOV DI, OFFSET RES
CODE LOOP:
          MOV AL,[SI]
          MOV CS,[DF],AL
          INC SI
          INC DI
          LOOP CODE LOOP
CODE ENDS
END START

